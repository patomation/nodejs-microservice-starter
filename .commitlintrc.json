{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "scope-enum": [
      2,
      "always",
      [
        "api",
        "app",
        "datasource",
        "deps",
        "dev",
        "doc",
        "docker",
        "entities",
        "routes",
        "server",
        "typescript"
      ]
    ]
  }
}
